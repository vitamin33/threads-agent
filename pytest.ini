# pytest.ini
[pytest]
# -r a  → “report on all tests”
# -q    → quiet output
addopts = -ra -q

testpaths =
    services
    tests

norecursedirs =
    templates

markers =
    e2e: end-to-end tests that spin k3d (slow)
