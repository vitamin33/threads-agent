# requirements.txt
fastapi~=0.111           # tiny health stub
uvicorn[standard]~=0.29  # ASGI server
pydantic>=2.7.0          # data models (FastAPI peer) - v2 for compatibility
pydantic-settings>=2.0.0 # Settings management
requests>=2.31           # already used by linear_mark_done.py
celery~=5.4
kombu~=5.3
cachetools>=5.3.0        # LRU cache for viral_engine
scikit-learn>=1.3.0      # ML for engagement predictor
numpy>=1.24.0           # Numerical computations
nltk>=3.8.1             # Natural language processing

# Revenue service dependencies
stripe>=7.12.0           # Stripe payment processing
email-validator>=2.1.1   # Email validation for lead capture
psycopg2-binary>=2.9.9   # PostgreSQL adapter
alembic>=1.13.1          # Database migrations

# MLflow experiment tracking
mlflow>=2.9.0            # Experiment tracking for LLM calls
psutil>=5.9.0            # System and process monitoring for performance tests
redis>=5.0.0             # Redis client for caching in fine-tuning pipeline
