# tests/requirements.txt
pytest==8.4.*                 # ≥ 8.3.3 to satisfy pytest-env
pytest-cov==4.*
pytest-xdist==3.5.*           # Parallel test execution
pytest-timeout==2.3.*         # Prevent hanging tests
httpx==0.28.*
pytest-asyncio==0.24.*        # 0.24 adds official support for pytest ≥ 8.3
qdrant-client>=1.9
psycopg2-binary==2.9.*         # For direct database access in e2e tests
fakeredis>=2.23
prometheus-client>=0.22.1
psutil>=5.9.0                  # For memory and CPU monitoring in performance tests
